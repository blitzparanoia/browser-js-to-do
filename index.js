let input = prompt('what would you like  to do?');
const todos= [thing one, thing two];

while (input !== 'quit' && input !== 'q') {
    if
    input = prompt ('what would yopu like to do?')
}
console.log('OK, you quit the app')